# server-compliance-checker
Automating the hardening process with a Python script that audits the server against a specific security checklist (e.g. a simplified CIS Benchmark) and generates a report showing what's good and what needs to be fixed.
